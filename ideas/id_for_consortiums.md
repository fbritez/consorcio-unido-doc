Status: done

En la vista detalle de los consorcios, es necesario en algun lado mostrar el di de el consorcio, si el backend no manda el valor, agregarlo al pyloar resonse siempre que se trata de un consorcio. 
Mostrarlo solo una vez, para fines de debugging. 

Prioriza implementacion simple y limpia dentro de el paradigma orientado a objetos. REutiliza todo lo posible. 

## Implementation Notes

Implemented and pushed to the following branches:
- `consorcio-unido`: `id-for-consortiums` (backend already supports sending ID in responses)
- `consorcio-unido.ui`: `id-for-consortiums` (frontend displays ID in consortium detail view)
